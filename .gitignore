# dependencies
node_modules/
# build/tmp
dist/
.cache/
*.log
# os
.DS_Store
Thumbs.db
# env / secrets — 절대 커밋 금지
.env
.env.*
!.env.example
# 초안(content/drafts/*.json)은 일일 PR 브랜치에 커밋되어 검토된다 → ignore 하지 않음

# 건강 사이트와 무관한 폴더(업로드/공개 금지)
deliverables/

# 배포 산출물
dist/
