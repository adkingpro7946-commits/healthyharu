# dependencies
node_modules/
# build/tmp
dist/
.cache/
*.log
# os
.DS_Store
Thumbs.db
# env / secrets — 절대 커밋 금지
.env
.env.*
!.env.example
# 초안(content/drafts/*.json)은 일일 PR 브랜치에 커밋되어 검토된다 → ignore 하지 않음
